# Citation Audit

Generated by `scripts/extract_citation_audit.py`.

Files:

- `citation_contexts.tsv`: one row per cited key in each citation command, with local context and reviewer fields.
- `source_coverage.tsv`: one row per active citation key, with source artifacts found under `sources/`.
- `subagent_assignments.md`: current review batches for delegated citation checks.

Review statuses for `citation_contexts.tsv`:

- `pending`: not reviewed yet.
- `supported`: citation reasonably supports the local claim.
- `weak`: citation is relevant but the prose overstates or misplaces it.
- `mismatch`: citation does not support the local claim.
- `metadata`: bibliographic/source metadata issue rather than a prose issue.

Current active citation contexts: 84.
Current unique active citation keys: 65.
Introduction citation contexts marked high priority: 13.
